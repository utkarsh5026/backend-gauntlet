"""Project 05 — a time-series metrics pipeline built from the point up.

See `SPEC.md`. The verticals live in `parse.py` (V1), `rollup.py` (V2),
`sink.py` (V3) and `sse.py` (V4); everything else — `broker.py`, `pipeline.py`,
`routes.py`, `main.py` — is wiring.
"""

__all__ = ["__version__"]

__version__ = "0.1.0"
