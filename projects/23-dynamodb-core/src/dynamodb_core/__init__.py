"""Project 23 — a DynamoDB data plane built from the item up.

See `SPEC.md`. The verticals live in `table.py` (V1), `indexes.py` (V2),
`conditions.py` (V3), `throughput.py` (V4) and `streams.py` (V5); everything else
is wiring. Distribution is deliberately out of scope — that's projects 07 and 09.
"""

__all__ = ["__version__"]

__version__ = "0.1.0"
