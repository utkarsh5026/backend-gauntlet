"""Project 02 — a distributed rate limiter.

See `SPEC.md`. The verticals live in `token_bucket.py` (V1),
`sliding_window.py` (V2) and `redis_limiter.py` (V3); everything else is wiring.
"""

__all__ = ["__version__"]

__version__ = "0.1.0"
