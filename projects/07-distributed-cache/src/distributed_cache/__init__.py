"""Project 07 — a distributed cache built from the ring up.

See `SPEC.md`. The verticals live in `store.py` (V1), `ring.py` (V2),
`membership.py` (V3) and `coordinator.py` (V4); everything else is wiring.
"""

__all__ = ["__version__"]

__version__ = "0.1.0"
