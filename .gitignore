/target
**/*.rs.bk
.env
*.log
.DS_Store

# Python (task runners / bench harness)
__pycache__/
*.py[cod]
.venv/
.ruff_cache/
.mypy_cache/
.pytest_cache/
.hypothesis/
# uv.lock IS committed — it is the Python side's Cargo.lock.

# Project 08's broker state: the filesystem IS the log, so `make run` lays
# down segment files under DATA_DIR. Never commit them.
projects/*/data/

# Project 24's sandbox scratch: one directory per execution environment under
# SANDBOX_ROOT, created on cold start and removed when the environment is reaped.
projects/*/run/
